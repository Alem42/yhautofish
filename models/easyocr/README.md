# EasyOCR Models

Put the EasyOCR runtime models here before packaging:

```text
craft_mlt_25k.pth
zh_sim_g2.pth
```

`make_python_exe.bat` can copy them from `%USERPROFILE%\.EasyOCR\model` when they already exist there.
